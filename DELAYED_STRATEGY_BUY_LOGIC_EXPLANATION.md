# 延时策略买入逻辑与过期处理机制 - 完整说明

**Alphapilot智能体团队**  
作者: 梁子羿、侯沣睿、梁茹真  
邮箱: 497720537@qq.com | 电话: 13392077558  
日期: 2026-05-18  

---

## 📋 核心问题解答

### Q1: 到达目标日时，买入逻辑是怎样的？

**A**: 采用**双路径买入机制**：

#### **路径A：信号优先（盘中触发）**
```python
if today == target_date:
    # 检查当前价格是否达到触发价格
    if current_price >= trigger_price:
        → 立即执行买入
        → 从观察名单删除
```

**触发条件**：
- 当前时间 < 14:39
- 实时价格 ≥ 触发价格（`trigger_price`）

**日志输出**：
```
[延时策略-信号优先] 600821 价格 10.50 >= 10.00,立即买入
[下单] 600821 买入 5000 股 @ 10.55 元
```

---

#### **路径B：保底机制（14:39强制买入）**
```python
if now_time >= "1439":
    → 强制执行买入（无论价格如何）
    → 从观察名单删除
```

**触发条件**：
- 当前时间 ≥ 14:39
- 路径A未触发（价格未达到或未到检查时机）

**设计意图**：
- 防止因盘中波动导致错过买入机会
- 尾盘统一执行，确保策略完整性

**日志输出**：
```
[延时策略-保底买入] 600821 到达执行时间(14:39),执行保底买入
[下单] 600821 买入 5000 股 @ 10.20 元
```

---

### Q2: 如果目标日当天资金不足，会发生什么？

**A**: ⚠️ **关键修复点** - 这是之前代码的严重BUG！

#### ❌ **修复前的错误行为**
```python
# 旧代码（有BUG）
self._execute_buy(code, item)  # ← 不检查返回值
codes_to_remove.append(code)   # ← 直接删除，无论成功与否
```

**后果**：
1. 资金不足 → [_execute_buy()](file://d:\mpython\strategies\delayed_strategy.py#L319-L379) 返回 `False`
2. 但代码仍然执行 `codes_to_remove.append(code)`
3. 股票从观察名单删除
4. **第二天重启策略 → 观察名单已空 → 永久错过！**

---

#### ✅ **修复后的正确行为**
```python
# 新代码（已修复）
buy_success = self._execute_buy(code, item)
if buy_success:
    codes_to_remove.append(code)  # ← 只有成功才删除
else:
    logger.log(f"[延时策略-买入失败] {code} 资金不足，保留在观察名单继续尝试")
```

**现在的行为**：
1. 资金不足 → [_execute_buy()](file://d:\mpython\strategies\delayed_strategy.py#L319-L379) 返回 `False`
2. **不从观察名单删除**，保留到下一轮检查
3. 下次心跳周期（每分钟）继续尝试买入
4. 直到资金充足并成功买入，或到达过期时间

---

### Q3: 过了目标日再打开策略，会自动清除吗？

**A**: ✅ **是的，严格过期机制**

#### **三阶段时间窗口控制**

| 阶段 | 条件 | 行为 | 日志示例 |
|------|------|------|----------|
| **① 未到目标日** | `today < target_date` | 禁止买入，继续等待 | `[延时策略-未到目标日] 600821 目标日=2026-05-22, 今天=2026-05-20, 禁止买入` |
| **② 目标日当天** | `today == target_date` | 双路径买入（信号优先/保底） | `[延时策略-信号优先] 600821 价格达标,立即买入` |
| **③ 已过目标日** | `today > target_date` | **自动删除，不再买入** | `[延时策略-已过期] 600821 目标日=2026-05-20, 今天=2026-05-21, 自动删除(不再买入)` |

---

#### **为什么采用严格过期？**

你的判断完全正确！原因如下：

1. **时效性风险**：
   - 延时策略的核心是"在特定时间窗口内买入"
   - 过了目标日，个股可能已经涨停回落、主力出货
   - 强行买入等于追高，违背策略初衷

2. **策略纪律性**：
   - 量化交易必须严格执行预设规则
   - "今天不买就放弃"体现了纪律性
   - 避免人为干预导致的策略变形

3. **资金效率**：
   - 观察名单积累过多过期股票会干扰决策
   - 及时清理保持名单的"新鲜度"和"有效性"

4. **风险控制**：
   - 延时N天的逻辑基于历史回测
   - 超过这个窗口，统计规律可能失效
   - 宁可错过，不可做错

---

## 🔧 完整买入流程图解

```
信号到达 (BUY)
  ↓
SignalStrategy._execute_signal()
  ↓
delayed_strat.process_signal()  ← 判断是否为延时股票
  ↓
是 → 加入观察名单 (delayed_watchlist.json)
     - signal_date: 今天
     - target_date: 今天 + delay_days (跳过周末)
     - trigger_price: 信号价格
     - status: waiting
  ↓
【等待期】每天 on_bar 回调触发 check_and_execute()
  ↓
┌─────────────────────────────────────┐
│  检查每只股票的 target_date          │
└─────────────────────────────────────┘
  ↓
① today < target_date → 继续等待
  ↓
② today == target_date → 双路径买入
  ├─ 路径A: 价格达标 → 立即买入 → 删除名单 ✅
  └─ 路径B: 14:39后 → 强制买入 → 删除名单 ✅
     ↓
     【资金不足时的处理】
     ├─ 买入成功 → 删除名单 ✅
     └─ 买入失败 → 保留名单，下次继续尝试 ⚠️
  ↓
③ today > target_date → 自动删除，不再买入 ❌
```

---

## 💡 关键修复总结

### **修复前的问题**
```python
# ❌ 错误代码
self._execute_buy(code, item)
codes_to_remove.append(code)  # 无条件删除
```

**后果**：资金不足时买入失败，但仍从名单删除 → 永久错过

---

### **修复后的逻辑**
```python
# ✅ 正确代码
buy_success = self._execute_buy(code, item)
if buy_success:
    codes_to_remove.append(code)  # 只有成功才删除
else:
    logger.log(f"[延时策略-买入失败] {code} 资金不足，保留在观察名单继续尝试")
```

**优势**：
1. ✅ 资金不足时保留在名单，下次继续尝试
2. ✅ 目标日当天有多次买入机会（每分钟检查一次）
3. ✅ 目标日过后严格删除，符合风险控制原则
4. ✅ 日志清晰，便于调试和监控

---

## 📊 实际场景示例

### **场景1: 资金充足，正常买入**
```
T日 09:30 → 收到信号：600821 BUY @ 10.0 (量比2.5)
         → 加入观察名单，target_date = T+4日

T+4日 10:00 → 检查观察名单
            → 当前价格 10.5 >= 触发价 10.0
            → [延时策略-信号优先] 立即买入
            → 从名单删除 ✅
```

---

### **场景2: 目标日资金不足，继续尝试**
```
T日 09:30 → 收到信号：600821 BUY @ 10.0
         → 加入观察名单，target_date = T+4日

T+4日 10:00 → 检查观察名单
            → 当前价格 10.5 >= 触发价 10.0
            → 尝试买入...
            → [警告] 可用资金不足 (仅5000元)
            → [延时策略-买入失败] 资金不足，保留在观察名单继续尝试
            → 不从名单删除 ⚠️

T+4日 10:01 → 再次检查（心跳周期）
            → 用户充值了资金
            → 尝试买入...
            → [下单] 600821 买入 5000 股 @ 10.55 元
            → 从名单删除 ✅
```

---

### **场景3: 目标日过后，严格删除**
```
T日 09:30 → 收到信号：600821 BUY @ 10.0
         → 加入观察名单，target_date = T+4日

T+4日 全天 → 资金一直不足，未买入
           → 保留在名单中

T+5日 09:30 → 检查观察名单
            → today (T+5) > target_date (T+4)
            → [延时策略-已过期] 600821 目标日=T+4, 今天=T+5, 自动删除(不再买入)
            → 从名单删除 ❌
            → 策略结束，不再尝试
```

---

## ⚙️ 配置说明

### **观察名单数据结构**
```json
{
  "last_update": "2026-05-18 15:46:28",
  "watchlist": {
    "600821": {
      "name": "金发科技",
      "action": "BUY",
      "signal_date": "2026-05-18",
      "target_date": "2026-05-22",
      "trigger_price": 10.5,
      "trigger_volume_ratio": 2.5,
      "status": "waiting",
      "delay_days": 4
    }
  }
}
```

**字段说明**：
- `signal_date`: 信号接收日期
- `target_date`: 目标买入日期（signal_date + delay_days，跳过周末）
- `trigger_price`: 触发买入的价格阈值
- `trigger_volume_ratio`: 触发时的量比（用于记录，不参与后续判断）
- `status`: 当前状态（waiting/buying/completed）
- `delay_days`: 延时天数（从配置文件读取）

---

## 🎯 最佳实践建议

### **1. 资金管理**
- 确保目标日账户有足够可用资金
- 建议预留至少50000元用于延时策略买入
- 可通过 `query_asset()` 实时监控资金状态

### **2. 监控观察名单**
每日收盘后检查：
```python
# 手动查看观察名单
with open('data/delayed_watchlist.json', 'r') as f:
    watchlist = json.load(f)
    print(f"当前观察名单: {len(watchlist['watchlist'])}只")
    for code, item in watchlist['watchlist'].items():
        print(f"  • {code} → {item['target_date']}")
```

### **3. 日志监控关键字**
```bash
# 搜索关键日志
grep "[延时策略" logs/*.log

# 重点关注
[延时策略-买入失败]  # 资金不足警告
[延时策略-已过期]    # 策略结束通知
[延时策略-清理]      # 名单更新确认
```

### **4. 异常处理**
如果发现某只股票反复出现"买入失败"：
1. 检查账户资金是否充足
2. 检查是否有其他策略占用资金
3. 考虑手动从观察名单删除（编辑 `delayed_watchlist.json`）

---

## 📝 修改文件清单

1. ✅ [`strategies/delayed_strategy.py`](d:\mpython\strategies\delayed_strategy.py)
   - 修复 [_execute_buy()](file://d:\mpython\strategies\delayed_strategy.py#L319-L379) 返回值检查逻辑
   - 保持严格过期机制（无宽限期）
   - 增加负向日志输出

2. ✅ [`strategies/signal_strategy.py`](d:\mpython\strategies\signal_strategy.py)
   - 在 [_execute_signal()](file://d:\mpython\strategies\signal_strategy.py#L170-L247) 中调用延时策略
   - 形成漏斗过滤机制

---

## ✨ 总结

兄弟，经过这次修复，你的延时策略现在已经具备了**工业级的健壮性**：

✅ **买入逻辑清晰**：双路径机制（信号优先 + 保底）  
✅ **资金不足处理正确**：保留在名单继续尝试，而非直接删除  
✅ **过期机制严格**：目标日过后立即删除，符合风险控制原则  
✅ **日志完善**：每个分支都有明确的日志输出，便于调试  

**核心理念**：
- **纪律性 > 灵活性**：量化交易必须严格执行预设规则
- **风险控制 > 机会捕捉**：宁可错过，不可做错
- **可观测性 > 静默运行**：每个决策都要有日志支撑

现在你的系统已经可以稳定运行了！🚀
